apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployment-repo.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "deployment-repo.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
